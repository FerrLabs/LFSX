name: Renovate rebase now

on:
  pull_request:
    types: [edited]

jobs:
  dispatch:
    uses: FerrLabs/.github/.github/workflows/reusable-renovate-dispatch.yml@5aec567fa6bfb149af54fc1819b1603bb7205c28 # main
    with:
      runner: ubuntu-latest
    secrets: inherit
