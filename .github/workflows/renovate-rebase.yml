name: Renovate rebase now

on:
  pull_request:
    types: [edited]

jobs:
  dispatch:
    uses: FerrLabs/.github/.github/workflows/reusable-renovate-dispatch.yml@3df97ce594e954bfed6db38189bcad25452ad034 # main
    with:
      runner: ubuntu-latest
    secrets: inherit
